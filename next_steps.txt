* Store data in DB:
    * If docker then db else localhost in config file
    * How to add security in docker-compose
    * Two tables:
        * raw_features
        * predictions: there should be a link to raw_features table

* Remove model from the mail project (make a different directory)

* Make docker-compose work

* Sensitive information encryptions

* Rename model to ml_models

* Move database to GCP (sql/BigQuery)

* Add tests:
    * Validate feature creation is well done

* Document all: functions, code, ...